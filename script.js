//addition function
const add(a , b) {
    return a + b;
}

//subtraction function
const subtract(a , b) {
    return a - b;
}
//multiply function
const multiply(a ,b) {
    return a * b;
}

const divide(a ,b) {
    if (b === 0) {
    return "cannont divide by 0"
    } return a / b;
}